<%= form_for(@kick_category) do |f| %>
  <% if @kick_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@kick_category.errors.count, "error") %> prohibited this kick_category from being saved:</h2>

      <ul>
      <% @kick_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id %><br />
    <%= f.number_field :id %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
