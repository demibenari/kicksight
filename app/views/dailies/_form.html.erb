<%= form_for(@daily) do |f| %>
    <% if @daily.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@daily.errors.count, "error") %> prohibited this daily from being saved:</h2>

          <ul>
            <% @daily.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="field">
      <%= f.label :pledge_id %>
      <br>
      <%= f.number_field :pledge_id %>
    </div>
    <div class="field">
      <%= f.label :Push_date_id %>
      <br>
      <%= f.number_field :Push_date_id %>
    </div>
    <div class="field">
      <%= f.label :amount_backers_integer %>
      <br>
      <%= f.text_field :amount_backers_integer %>
    </div>
    <div class="field">
      <%= f.label :amount_pledged %>
      <br>
      <%= f.number_field :amount_pledged %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>
