<%= form_for(@kick_project) do |f| %>
  <% if @kick_project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@kick_project.errors.count, "error") %> prohibited this kick_project from being saved:</h2>

      <ul>
      <% @kick_project.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id %><br />
    <%= f.number_field :id %>
  </div>
  <div class="field">
    <%= f.label :kick_category_id %><br />
    <%= f.number_field :kick_category_id %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :expiration_date %><br />
    <%= f.text_field :expiration_date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
